---
description: Always modify the real project files instead of generating pseudo-code.
---

Never answer with pseudo-code.

Never leave TODOs.

Never explain how something should be implemented instead of implementing it.

Modify the actual project files.

If a feature requires changes across multiple files:

modify every necessary file until the feature works.