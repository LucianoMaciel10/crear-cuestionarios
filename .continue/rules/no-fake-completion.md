---
description: Only report features as completed when they are fully integrated.
---

Never assume a feature works because the project compiles.

A task is only complete if:

- it is integrated into the application
- it is reachable through the UI when applicable
- it follows the existing architecture
- it does not leave orphaned code

Be conservative when reporting completed work.

Do not produce optimistic summaries.