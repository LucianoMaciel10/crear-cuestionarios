---
description: Create commits only after the implementation is fully completed.
---

Only create a git commit when:

- implementation is finished
- documentation is updated
- build passes
- lint passes
- typecheck passes

Create a single commit.

Use a professional English commit message.

Never create partial commits.

And push the commit.